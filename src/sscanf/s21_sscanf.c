#include <stdarg.h>

#include "../string/s21_string.h"

int s21_sscanf(const char* str, const char* format, ...) {
  va_list args;
  va_start(args, format);

  int status = 0;

  // while proccess cycle

  va_end(args);

  return status;
}
